name: computer-vision-tool
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - streamlit>=1.39.0
    - mediapipe
    - protobuf<4.0.0
    - numpy<2.0.0
    - pandas
    - Pillow
    - requests
    - streamlit-webrtc
    - streamlit-lottie
    - av
    - aiortc
    - aioice
    # - protobuf<4.0.0  # Let pip resolve protobuf to avoid conflicts
